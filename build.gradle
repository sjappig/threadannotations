allprojects {
    version = '0.1'

    apply plugin: 'java'
    apply plugin: 'eclipse'

    repositories {
        jcenter()
    }

    task srcJar(type: Jar, dependsOn: classes) {
        classifier = 'src'
        from sourceSets.main.allSource
    }
}
